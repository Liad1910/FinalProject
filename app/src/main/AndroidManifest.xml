<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- הרשאות -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.FinalProject">

        <!-- Receiver לרשת -->
        <receiver
            android:name=".Network"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <!-- Activities -->
        <activity
            android:name=".NearbyCinemaFreeActivity"
            android:exported="false" />

        <activity
            android:name=".AiActivity"
            android:exported="false" />

        <activity
            android:name=".CreateTitleActivity"
            android:exported="false" />

        <activity
            android:name=".MovieContentActivity"
            android:exported="false" />

        <activity
            android:name=".MoviesCategoryActivity"
            android:exported="false" />

        <activity
            android:name=".SeriesCategoryActivity"
            android:exported="false" />

        <activity
            android:name=".activity_user_page"
            android:exported="false" />

        <activity
            android:name=".loginPage"
            android:exported="true" />

        <activity
            android:name=".registerPage"
            android:exported="true" />

        <!-- Launcher + Splash -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Watchly.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true" />


    </application>

</manifest>
